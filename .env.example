# Required in production — use a long random string
JWT_SECRET=change-this-to-a-long-random-secret

# Where SQLite DB and uploaded images are stored
DATA_DIR=./server

# Server port (Render/Railway set PORT automatically)
PORT=3001
